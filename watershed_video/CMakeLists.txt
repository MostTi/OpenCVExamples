cmake_minimum_required(VERSION 2.8)
project( watershed_video )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( watershed_video watershed_video.cpp )
target_link_libraries( watershed_video ${OpenCV_LIBS} )
